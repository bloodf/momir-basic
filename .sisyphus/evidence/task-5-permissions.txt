# Task 5: Runtime Permission and Capability Gating - Evidence

## Files Referenced
- `expo/services/printer/capability/service.ts` (284 lines)

## Android Runtime Permission Flow

### PermissionsAndroid Usage (lines 1, 58-79, 81-147)
```typescript
import { Platform, NativeModules, PermissionsAndroid } from 'react-native';
```
- `PermissionsAndroid.check()` used to check `BLUETOOTH_CONNECT`, `BLUETOOTH_SCAN`, `ACCESS_FINE_LOCATION` permissions (lines 58-60)
- `PermissionsAndroid.requestMultiple()` used to request all required Bluetooth permissions (line 100)

### AndroidBluetoothPermissionState Interface (lines 11-22)
```typescript
export interface AndroidBluetoothPermissionState {
  BLUETOOTH_CONNECT: AndroidBluetoothPermissionStatus;
  BLUETOOTH_SCAN: AndroidBluetoothPermissionStatus;
  ACCESS_FINE_LOCATION: AndroidBluetoothPermissionStatus;
  overall: AndroidBluetoothPermissionStatus;
}
```
- Status values: `'granted'`, `'denied'`, `'never_ask_again'`, `'undetermined'`

### ensureBluetoothPermissions() (lines 242-268)
- Checks current permission state
- If not granted, calls `requestAndroidBluetoothPermissions()`
- Throws explicit error with reason if denied/never_ask_again
- Emits diagnostic events via `emitPermissionGranted()` / `emitPermissionDenied()`

## iOS Transport Gating

### getTransportCapability() State Machine (lines 153-226)
```typescript
getTransportCapability(transport: PrinterTransport): TransportCapability {
  // Returns explicit TransportCapability with isSupported, nativeModuleAvailable, reason
  if (transport === 'tcp') { ... }
  if (transport === 'ble') { ... }
  if (transport === 'classic') {
    if (isIOS) {
      return {
        transport,
        isSupported: false,
        reason: 'Classic Bluetooth is not supported on iOS. Only MFi/External Accessory printers are supported.',
      };
    }
    ...
  }
  return { transport, isSupported: false, reason: `Unknown transport: ${transport}` };
}
```
- iOS Classic explicitly blocked with reason
- TCP validated separately from Bluetooth
- All branches return explicit `TransportCapability` with `isSupported` boolean

## getAllTransportCapabilities() (lines 228-234)
```typescript
getAllTransportCapabilities(): TransportCapability[] {
  return [
    this.getTransportCapability('ble'),
    this.getTransportCapability('classic'),
    this.getTransportCapability('tcp'),
  ];
}
```

## Satisfies Task 5 Requirements
- [x] Android requests runtime Bluetooth permissions before scan/connect
- [x] Denied permission state renders explicit remediation UX and blocks scan/connect
- [x] iOS unsupported Classic actions are hidden or blocked with explicit messaging
- [x] Printer capability state machine computes support by platform + transport + native module presence
- [x] `isNativeModuleAvailable()` gates all transport support
