# Task 1: Release Model and Versioning Policy - Evidence

## TypeScript Check
```
cd expo && bunx tsc --noEmit
Result: PASS (exit 0, no output)
```

## EAS Config Validation
```
cd expo && eas config --platform ios --profile production
Result: FAIL - "Invalid UUID appId" - EAS project not linked (expected, projectId is TODO)

cd expo && eas config --platform android --profile production
Result: FAIL - "Invalid UUID appId" - EAS project not linked (expected, projectId is TODO)
```

Note: `eas config` requires a linked EAS project for full server-side validation.
The `projectId` in `app.json` contains a TODO marker.
Run `cd expo && eas project:associate` to link your EAS project before full validation.

## eas.json Validation
```
cat expo/eas.json | jq .
Result: Valid JSON - PASS
```

## RELEASE_POLICY.md
```
test -f expo/docs/release/RELEASE_POLICY.md
Result: EXISTS
```

## Files Created/Modified
- expo/eas.json (created) - EAS Build/Submit profiles
- expo/app.json (updated) - Added cli.appVersionSource and extra.eas.projectId
- expo/docs/release/RELEASE_POLICY.md (created) - Full release policy documentation
