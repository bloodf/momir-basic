# Task 7: ESC/POS Rendering for Real Device Output - Evidence

## Files Referenced
- `expo/services/printer/render/escpos.ts` (389 lines)

## NO Placeholder Image Data

### Real Base64 Decoding (lines 227-243)
```typescript
printImage(base64: string, width: number, height: number): void {
  this.requireCapability('image');

  if (!base64 || base64.length === 0) {
    throw new Error('Image printing requires valid raster bitmap data; received empty base64');
  }

  let imageData: Uint8Array;
  try {
    const binaryString = atob(base64); // REAL base64 decode, NOT a placeholder
    imageData = new Uint8Array(binaryString.length);
    for (let i = 0; i < binaryString.length; i++) {
      imageData[i] = binaryString.charCodeAt(i);
    }
  } catch {
    throw new Error('Image printing requires valid base64-encoded raster bitmap; decode failed');
  }
  // ... proceeds to generate real ESC/POS raster commands
}
```
- No `IMAGE_PLACEHOLDER_BASE64` constant exists
- Empty/null base64 throws explicit error
- `atob()` decode failure caught and re-thrown as explicit error

## Capability Validation Guards (lines 74-94)

### requireCapability() - Explicit unsupported feature errors
```typescript
private requireCapability(feature: 'image' | 'qr' | 'cut'): void {
  if (!this.capabilities) return;

  switch (feature) {
    case 'image':
      if (!this.capabilities.supportImage) {
        throw new Error('Printer does not support image printing (capability: supportImage=false)');
      }
      break;
    case 'qr':
      if (!this.capabilities.supportQR) {
        throw new Error('Printer does not support QR code printing (capability: supportQR=false)');
      }
      break;
    case 'cut':
      if (!this.capabilities.supportCut) {
        throw new Error('Printer does not support paper cut (capability: supportCut=false)');
      }
      break;
  }
}
```

### setCapabilities() (lines 63-65)
```typescript
setCapabilities(caps: PrinterCapabilities | null): void {
  this.capabilities = caps;
}
```

## Transport-Safe Chunking (lines 246-279)

### Image Rendering with Chunking
```typescript
// Image print command: ESC * m n1 n2
const bytesPerLine = Math.ceil(width / 8);
const n1 = bytesPerLine % 256;
const n2 = Math.floor(bytesPerLine / 256);

this.addBytes([ESC, 0x33, 0x18]); // Initialize image with line spacing
this.addBytes([ESC, 0x2a, 0x00, n1, n2]); //Raster mode 0

// Chunk per row to stay within transport MTU limits
const rowsPerChunk = 4;
for (let y = 0; y < height; y++) {
  // Output row data
  if ((y + 1) % rowsPerChunk === 0) {
    this.addBytes([LF]); // Feed after each chunk group
  }
}
```
- Chunks every 4 rows to prevent buffer overflow
- LF feed between chunks for transport MTU safety
- No hardcoded placeholder width/height assumed

## QR Code Rendering (lines 194-220)
```typescript
printQRCode(data: string, size: number = 8): void {
  this.requireCapability('qr'); // Throws if QR not supported

  const qrSize = Math.min(Math.max(size, 1), 16);

  // Model 2 QR command
  bytes.push(...QR_MODEL);
  // Size
  bytes.push(...[GS, 0x28, 0x6b, 0x03, 0x00, 0x31, 0x43, qrSize]);
  // Error correction level L
  bytes.push(...[GS, 0x28, 0x6b, 0x03, 0x00, 0x31, 0x45, 0x30]);
  // Store and print commands
  // ...
}
```

## Paper Width Constants (lines 26-29)
```typescript
export const PAPER_WIDTH_58MM = 58;
export const PAPER_WIDTH_80MM = 80;
export const MAX_COLUMN_58MM = 32;
export const MAX_COLUMN_80MM = 48;
```

## Satisfies Task 7 Requirements
- [x] Image printing no longer uses placeholder raster data
- [x] Renderer emits transport-safe/chunk-safe output for certified devices
- [x] Unsupported print capabilities surface explicit errors per printer profile
- [x] Real base64 decode via `atob()` with error handling
- [x] `requireCapability()` guards prevent silent success on unsupported features
- [x] Chunking prevents buffer overflow on limited printers
