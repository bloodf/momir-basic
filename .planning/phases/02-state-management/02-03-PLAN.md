---
phase: 02-state-management
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - app/_layout.tsx
  - app/card.tsx
  - app/life-counter.tsx
  - app/print-preview.tsx
  - app/(tabs)/(home)/index.tsx
  - app/(tabs)/search/index.tsx
  - app/(tabs)/history/index.tsx
  - app/(tabs)/game/index.tsx
  - app/(tabs)/settings/index.tsx
  - app/(tabs)/settings/printer.tsx
  - components/HistorySheet.tsx
  - components/SearchFilters.tsx
  - components/SearchFiltersDialog.tsx
  - components/TypePicker.tsx
  - services/scryfall.ts
  - i18n/index.ts
autonomous: true
requirements: [ARCH-05, ARCH-07]
nyquist_compliant: true

must_haves:
  truths:
    - "Provider tree is 4 levels or fewer: composeProviders([QueryClientProvider wrapper, GestureHandlerRootView, SafeAreaProvider, ToastProvider]) -> App (D-06)"
    - "No provider wrappers for Settings, History, Network, or I18n remain in _layout.tsx (D-06)"
    - "All consumer imports reference stores/ instead of providers/ for settings, history, and network"
    - "All consumer imports reference stores/i18nStore instead of i18n/ for the useI18n hook"
    - "Layout updates immediately on device rotation in all 5 previously stale Dimensions.get call sites (ARCH-07)"
    - "No module-scope Dimensions.get('window') calls remain in app/ or components/ (ARCH-07)"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Flattened provider tree with composeProviders"
      contains: "composeProviders"
    - path: "services/scryfall.ts"
      provides: "Replaced buildQuery/getTypeQueryFragment with CARD_TYPE_QUERIES"
    - path: "i18n/index.ts"
      provides: "Cleaned up: removed I18nProvider, kept locale data and types"
  key_links:
    - from: "app/_layout.tsx"
      to: "utils/composeProviders.tsx"
      via: "import { composeProviders } from '@/utils/composeProviders'"
      pattern: "composeProviders"
    - from: "app/_layout.tsx"
      to: "stores/networkStore.ts"
      via: "import { initNetworkListener } for side-effect initialization"
      pattern: "initNetworkListener"
    - from: "app/(tabs)/(home)/index.tsx"
      to: "stores/historyStore.ts"
      via: "import { useHistoryStore, useFilteredHistory } from '@/stores/historyStore'"
      pattern: "useHistoryStore"
    - from: "services/scryfall.ts"
      to: "constants/cardTypes.ts"
      via: "import { CARD_TYPE_QUERIES } from '@/constants/cardTypes'"
      pattern: "CARD_TYPE_QUERIES"
    - from: "components/CardGridItem.tsx"
      to: "useWindowDimensions"
      via: "const { width } = useWindowDimensions() inside component"
      pattern: "useWindowDimensions"
---

<objective>
Wire all Zustand stores into the app by updating consumer imports, flattening the provider tree to 4 levels, replacing the Scryfall switch statements with CARD_TYPE_QUERIES, and replacing all module-scope Dimensions.get calls with useWindowDimensions.

Purpose: This is the integration plan that connects all store and utility work from Plans 01 and 02 to the actual app. After this plan, the app runs entirely on Zustand + MMKV + NetInfo with a shallow provider tree, and all layout responds to rotation/resize.
Output: Flattened _layout.tsx, updated consumer imports, CARD_TYPE_QUERIES wired into scryfall.ts, responsive layouts in 5 files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management/02-CONTEXT.md
@.planning/phases/02-state-management/02-UI-SPEC.md
@.planning/phases/02-state-management/02-01-SUMMARY.md
@.planning/phases/02-state-management/02-02-SUMMARY.md
@stores/settingsStore.ts
@stores/historyStore.ts
@stores/networkStore.ts
@stores/i18nStore.ts
@utils/composeProviders.tsx
@constants/cardTypes.ts
@app/_layout.tsx

<interfaces>
<!-- New store APIs replacing provider hooks. All consumers must switch to these. -->

From stores/settingsStore.ts:
```typescript
export const useSettingsStore = create<SettingsState>()(persist(...));
// SettingsState: { settings, updateSettings, updatePrinter, savePreferredPrinter, getPreferredPrinter }
// NO isLoading property (MMKV is synchronous)
```

From stores/historyStore.ts:
```typescript
export const useHistoryStore = create<HistoryState>()(persist(...));
export function useFilteredHistory(search: string, typeFilter?: string, cmcFilter?: number): Card[];
// HistoryState: { cards, addCard, addCards, removeCard, clearHistory }
// NO isLoading property
```

From stores/networkStore.ts:
```typescript
export const useNetworkStore = create<NetworkState>()();
export function initNetworkListener(): void;
export function cleanupNetworkListener(): void;
// NetworkState: { isOnline, isReachable, checkNow }
// NO isChecking property (NetInfo is event-driven, not polling)
```

From stores/i18nStore.ts:
```typescript
export const useI18nStore = create<I18nState>()(persist(...));
export function useI18n(): { locale, setLocale, t, scryfallLang };
// Re-exports: LOCALE_LABELS, LOCALE_FLAGS, ALL_LOCALES, LOCALE_TO_SCRYFALL_LANG, Locale type
// NO loaded property (MMKV is synchronous)
```

From constants/cardTypes.ts:
```typescript
export const CARD_TYPE_QUERIES: Record<CardType, CardTypeQuery>;
// CardTypeQuery: { typeFragment, buildQuery, useCmc }
```

From utils/composeProviders.tsx:
```typescript
export function composeProviders(providers: React.ComponentType<{children}>[]): React.ComponentType<{children}>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten _layout.tsx + update all consumer imports + wire CARD_TYPE_QUERIES</name>
  <files>app/_layout.tsx, app/card.tsx, app/life-counter.tsx, app/print-preview.tsx, app/(tabs)/(home)/index.tsx, app/(tabs)/search/index.tsx, app/(tabs)/history/index.tsx, app/(tabs)/game/index.tsx, app/(tabs)/settings/index.tsx, app/(tabs)/settings/printer.tsx, components/HistorySheet.tsx, components/SearchFilters.tsx, components/SearchFiltersDialog.tsx, components/TypePicker.tsx, services/scryfall.ts, i18n/index.ts</files>
  <read_first>app/_layout.tsx, app/card.tsx, app/life-counter.tsx, app/print-preview.tsx, app/(tabs)/(home)/index.tsx, app/(tabs)/search/index.tsx, app/(tabs)/history/index.tsx, app/(tabs)/game/index.tsx, app/(tabs)/settings/index.tsx, app/(tabs)/settings/printer.tsx, components/HistorySheet.tsx, components/SearchFilters.tsx, components/SearchFiltersDialog.tsx, components/TypePicker.tsx, services/scryfall.ts, i18n/index.ts, stores/settingsStore.ts, stores/historyStore.ts, stores/networkStore.ts, stores/i18nStore.ts, utils/composeProviders.tsx, constants/cardTypes.ts</read_first>
  <acceptance_criteria>
    - `grep -r "from.*providers/SettingsProvider" app/ components/` returns no results
    - `grep -r "from.*providers/HistoryProvider" app/ components/` returns no results
    - `grep -r "from.*providers/NetworkProvider" app/ components/` returns no results
    - `grep -r "from.*@/i18n" app/_layout.tsx` returns no results (i18n removed from layout)
    - `grep -r "SettingsProvider\|HistoryProvider\|NetworkProvider\|I18nProvider" app/_layout.tsx` returns no results
    - `grep "composeProviders" app/_layout.tsx` returns exactly one match
    - `grep "initNetworkListener" app/_layout.tsx` returns at least one match
    - `grep "CARD_TYPE_QUERIES" services/scryfall.ts` returns at least one match
    - `grep "buildQuery\|getTypeQueryFragment" services/scryfall.ts` returns no function definitions (replaced by map)
    - `bun run test` passes (existing tests)
    - `bunx tsc --noEmit` passes
  </acceptance_criteria>
  <action>
1. Update `app/_layout.tsx` -- flatten provider tree (D-06, D-07, ARCH-05):
   a. Remove imports of: HistoryProvider, SettingsProvider, NetworkProvider, I18nProvider, useSettings, useI18n from their old locations
   b. Add imports:
      - `import { composeProviders } from '@/utils/composeProviders'`
      - `import { initNetworkListener } from '@/stores/networkStore'`
      - `import { useSettingsStore } from '@/stores/settingsStore'`
      - `import { useI18n } from '@/stores/i18nStore'`
      - `import { ToastProvider } from '@/components/Toast'`
   c. Create provider wrappers for providers that need props:
      ```typescript
      const QueryProvider = ({ children }: { children: React.ReactNode }) => (
        <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
      );
      ```
   d. Replace the deeply nested provider tree with:
      ```typescript
      const AppProviders = composeProviders([
        QueryProvider,
        GestureHandlerRootView,
        SafeAreaProvider,
        ToastProvider,
      ]);
      ```
   e. The return statement becomes:
      ```typescript
      return (
        <AppProviders>
          <PrinterAutoConnect />
          <RootLayoutNav />
        </AppProviders>
      );
      ```
   f. Update PrinterAutoConnect to use store hooks directly:
      - `const { settings } = useSettingsStore()` instead of `const { settings } = useSettings()`
      - `const { t } = useI18n()` instead of `const { t } = useI18n()` from i18n (same API, different import)
   g. Initialize network listener -- call `initNetworkListener()` once on mount:
      - Add a useEffect in RootLayout (or PrinterAutoConnect) that calls initNetworkListener on mount and cleanupNetworkListener on unmount
      - Best location: a new small useEffect in RootLayout itself:
        ```typescript
        useEffect(() => {
          initNetworkListener();
          return cleanupNetworkListener;
        }, []);
        ```
   h. Remove `import { useSettings } from "@/providers/SettingsProvider"` and `import { useI18n } from "@/i18n"` from _layout.tsx

2. Update ALL consumer files to import from stores/ instead of providers/:

   For each file that imports `useSettings` from `@/providers/SettingsProvider`:
   - Replace with: `import { useSettingsStore } from '@/stores/settingsStore'`
   - Replace `useSettings()` calls with `useSettingsStore()` destructuring
   - Note: API is nearly identical. Key differences:
     - No `isLoading` property (remove any destructuring of isLoading from useSettings)
     - `savePreferredPrinter` and `getPreferredPrinter` are on the store directly

   For each file that imports `useHistory` or `useFilteredHistory` from `@/providers/HistoryProvider`:
   - Replace with: `import { useHistoryStore, useFilteredHistory } from '@/stores/historyStore'`
   - Replace `useHistory()` calls with `useHistoryStore()` destructuring
   - `useFilteredHistory` keeps the same signature (import from stores/historyStore)

   For each file that imports `useNetwork` from `@/providers/NetworkProvider`:
   - Replace with: `import { useNetworkStore } from '@/stores/networkStore'`
   - Replace `useNetwork()` calls with `useNetworkStore()` destructuring
   - Note: No `isChecking` property. If any consumer destructures `isChecking`, remove it.

   For each file that imports `useI18n` from `@/i18n`:
   - Replace with: `import { useI18n } from '@/stores/i18nStore'`
   - The useI18n() hook has the same API: { locale, setLocale, t, scryfallLang }
   - No `loaded` property. If any consumer checks `loaded`, remove the check.

   For settings/index.tsx which imports LOCALE_LABELS, LOCALE_FLAGS, ALL_LOCALES, Locale type from `@/i18n`:
   - Replace with: `import { LOCALE_LABELS, LOCALE_FLAGS, ALL_LOCALES, type Locale } from '@/stores/i18nStore'`

   Specific file changes (14 consumer files):
   - `app/_layout.tsx`: Already handled in step 1
   - `app/card.tsx`: Change 3 imports (useHistory, useSettings, useI18n)
   - `app/life-counter.tsx`: Change 3 imports (useI18n, useHistory, useSettings)
   - `app/print-preview.tsx`: Change 2 imports (useSettings, useI18n)
   - `app/(tabs)/(home)/index.tsx`: Change 4 imports (useHistory, useSettings, useI18n, useNetwork)
   - `app/(tabs)/search/index.tsx`: Change 1 import (useI18n)
   - `app/(tabs)/history/index.tsx`: Change 2 imports (useHistory, useFilteredHistory, useI18n)
   - `app/(tabs)/game/index.tsx`: Change 1 import (useI18n)
   - `app/(tabs)/settings/index.tsx`: Change 2 imports (useSettings, useI18n + LOCALE exports)
   - `app/(tabs)/settings/printer.tsx`: Change 2 imports (useSettings, useI18n)
   - `components/HistorySheet.tsx`: Change 2 imports (useHistory, useI18n)
   - `components/SearchFilters.tsx`: Change 1 import (useI18n)
   - `components/SearchFiltersDialog.tsx`: Change 1 import (useI18n)
   - `components/TypePicker.tsx`: Change 1 import (useI18n)

3. Update `services/scryfall.ts` to use CARD_TYPE_QUERIES (ARCH-06):
   a. Add import: `import { CARD_TYPE_QUERIES } from '@/constants/cardTypes'`
   b. Replace `buildQuery` function (lines 125-150) with:
      ```typescript
      function buildQuery(cardType: CardType, cmc: number, excludeFunny: boolean): string {
        return CARD_TYPE_QUERIES[cardType].buildQuery(cmc, excludeFunny);
      }
      ```
   c. Replace `getTypeQueryFragment` function (lines 152-165) with:
      ```typescript
      function getTypeQueryFragment(cardType: CardType): string {
        return CARD_TYPE_QUERIES[cardType].typeFragment;
      }
      ```
   d. Keep the function signatures and names the same -- they're called internally by other scryfall.ts functions. The single source of truth is now CARD_TYPE_QUERIES, but the wrapper functions maintain backward compatibility within the module.

4. Update `i18n/index.ts` -- remove provider wrapper, keep locale data:
   a. Remove the `createContextHook` import and the I18nProvider/useI18n provider definition (lines 108-145)
   b. Remove the `AsyncStorage` import (no longer used here)
   c. Keep ALL other exports: Locale type, Translations type, LOCALE_TO_SCRYFALL_LANG, LOCALE_LABELS, LOCALE_FLAGS, ALL_LOCALES, locales record, getDeviceLocale function
   d. These will be imported by stores/i18nStore.ts and re-exported from there
   e. The file becomes a pure data/config module with no React dependencies

5. Run TypeScript check: `bunx tsc --noEmit` to verify all imports resolve correctly
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun run test</automated>
  </verify>
  <done>
    - app/_layout.tsx uses composeProviders with 4 providers (QueryClient, GestureHandler, SafeArea, Toast) + Zustand stores imported at module level
    - No provider wrappers for Settings, History, Network, or I18n in _layout.tsx
    - initNetworkListener called in useEffect on mount
    - All 14 consumer files import from stores/ instead of providers/
    - No consumer references providers/SettingsProvider, providers/HistoryProvider, providers/NetworkProvider
    - useI18n imported from @/stores/i18nStore (not @/i18n) in all consumers
    - services/scryfall.ts uses CARD_TYPE_QUERIES instead of switch statements
    - i18n/index.ts is a pure data module (no provider, no AsyncStorage)
    - TypeScript compiles without errors
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Dimensions.get with useWindowDimensions in 5 files</name>
  <files>app/card.tsx, app/life-counter.tsx, app/print-preview.tsx, components/CardGridItem.tsx, components/HistorySheet.tsx</files>
  <read_first>app/card.tsx, app/life-counter.tsx, app/print-preview.tsx, components/CardGridItem.tsx, components/HistorySheet.tsx, .planning/phases/02-state-management/02-RESEARCH.md (Pattern 6, Pitfall 6), .planning/phases/02-state-management/02-UI-SPEC.md (Section 2: Responsive Layout)</read_first>
  <acceptance_criteria>
    - `grep -r "Dimensions.get" app/ components/` returns no results (zero stale dimension calls)
    - `grep "useWindowDimensions" app/card.tsx` returns at least one match
    - `grep "useWindowDimensions" app/life-counter.tsx` returns at least one match
    - `grep "useWindowDimensions" app/print-preview.tsx` returns at least one match
    - `grep "useWindowDimensions" components/CardGridItem.tsx` returns at least one match
    - `grep "useWindowDimensions" components/HistorySheet.tsx` returns at least one match
    - `bunx tsc --noEmit` passes
    - `bun run test` passes
  </acceptance_criteria>
  <action>
For each of the 5 files, apply the same pattern (per RESEARCH.md Pitfall 6 and UI-SPEC Section 2):

General rules:
- Replace module-scope `Dimensions.get('window')` with `useWindowDimensions()` hook inside the component body
- Move dimension-dependent constants from module scope into the component, computed from hook values
- Move dimension-dependent styles from module-scope `StyleSheet.create()` to inline computed styles (useMemo-wrapped) within the component
- Keep non-dimension-dependent styles in StyleSheet.create() at module scope
- Import `useWindowDimensions` from 'react-native' (already available, no new dependency)
- Remove `Dimensions` import if no longer used

1. `components/CardGridItem.tsx` (line 12):
   - Current: `const SCREEN_WIDTH = Dimensions.get('window').width;`
   - Current: Uses SCREEN_WIDTH to compute CARD_WIDTH at module scope
   - Replace: Inside component, `const { width: screenWidth } = useWindowDimensions();`
   - Compute card width as useMemo: `const cardWidth = useMemo(() => (screenWidth - 16 * 2 - 8) / 2, [screenWidth]);`
   - Move CARD_WIDTH dependent styles from StyleSheet.create to inline (use cardWidth in style prop)
   - Remove module-scope SCREEN_WIDTH constant

2. `components/HistorySheet.tsx` (line 25):
   - Current: `const { height: SCREEN_HEIGHT } = Dimensions.get('window');`
   - Replace: Inside component, `const { height: screenHeight } = useWindowDimensions();`
   - Move height-dependent styles to inline computation
   - Remove module-scope SCREEN_HEIGHT constant

3. `app/card.tsx` (line 58):
   - Current: `const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');`
   - Replace: Inside component, `const { width: screenWidth, height: screenHeight } = useWindowDimensions();`
   - Move dimension-dependent constants (like HERO_HEIGHT = screenHeight * 0.48) into useMemo
   - Move dimension-dependent styles from StyleSheet.create to inline
   - Remove module-scope constants

4. `app/life-counter.tsx` (line 38):
   - Current: `const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');`
   - Replace: Inside component, `const { width: screenWidth, height: screenHeight } = useWindowDimensions();`
   - Same pattern as card.tsx

5. `app/print-preview.tsx` (line 33):
   - Current: `const { width: SCREEN_WIDTH } = Dimensions.get('window');`
   - Replace: Inside component, `const { width: screenWidth } = useWindowDimensions();`
   - Same pattern

After all changes, verify:
- `grep -r "Dimensions.get" app/ components/` returns ZERO results
- Remove `Dimensions` from react-native imports in files that no longer use it
- `bunx tsc --noEmit` passes (no type errors from moving styles)
- `bun run test` passes (existing tests still work)
  </action>
  <verify>
    <automated>grep -r "Dimensions.get" app/ components/ && echo "FAIL: stale Dimensions.get found" || echo "PASS: no stale Dimensions.get"; bunx tsc --noEmit && bun run test</automated>
  </verify>
  <done>
    - Zero `Dimensions.get('window')` calls in app/ and components/ directories
    - All 5 files use `useWindowDimensions()` hook inside component bodies
    - Dimension-dependent styles moved to inline useMemo computations
    - Non-dimension-dependent styles remain in StyleSheet.create at module scope
    - No `Dimensions` import remains in files that no longer use it
    - TypeScript compiles without errors
    - All existing tests pass
    - Layout responds to rotation/resize (reactive, not stale)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stores -> consumers | Zustand store imports are module-level; no trust boundary within the app |
| Consumer code -> stores | Selector pattern prevents over-exposure; consumers access only what they select |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | E (Elevation) | _layout.tsx provider removal | accept | Zustand stores are module-level singletons; no privilege elevation possible; stores are read-only from consumer perspective via selectors |
| T-02-07 | I (Information Disclosure) | Dimensions from useWindowDimensions | accept | Window dimensions are device-level info, not sensitive; no PII exposed |
</threat_model>

<verification>
1. `grep -r "Dimensions.get" app/ components/` returns zero results (ARCH-07)
2. `grep -r "SettingsProvider\|HistoryProvider\|NetworkProvider\|I18nProvider" app/_layout.tsx` returns zero results (ARCH-05)
3. `grep "composeProviders" app/_layout.tsx` returns exactly one match (D-07)
4. `bunx tsc --noEmit` passes (type safety maintained)
5. `bun run test` passes (no regressions)
6. Manual: inspect app/_layout.tsx and count provider nesting depth (must be 4 or fewer)
</verification>

<success_criteria>
- Provider tree is 4 levels: composeProviders([QueryProvider, GestureHandlerRootView, SafeAreaProvider, ToastProvider]) -> App
- No provider wrappers for stores (Settings, History, Network, I18n)
- All consumer imports reference stores/ (not providers/)
- services/scryfall.ts delegates to CARD_TYPE_QUERIES (no switch statements for card types)
- i18n/index.ts is a pure data module (no React provider code)
- Zero Dimensions.get('window') calls in app/ and components/
- All 5 affected components use useWindowDimensions() hook
- TypeScript compiles without errors
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-management/02-03-SUMMARY.md`
</output>