---
phase: 01-stability-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/logger.ts
  - __tests__/utils/logger.test.ts
  - services/scryfall.ts
  - providers/NetworkProvider.tsx
  - providers/SettingsProvider.tsx
  - providers/HistoryProvider.tsx
  - i18n/index.ts
  - app/(tabs)/settings/index.tsx
  - app/(tabs)/settings/printer.tsx
  - app/(tabs)/(home)/index.tsx
  - app/card.tsx
  - app/print-preview.tsx
  - services/printer/adapters/native.ts
  - services/printer/adapters/fake.ts
  - jest.setup.js
autonomous: true
requirements:
  - STAB-04

must_haves:
  truths:
    - "Every empty catch block in the codebase either handles the error explicitly or logs it with a classified error type"
    - "All console.log calls in production code are replaced with structured logger calls"
    - "Logger is a no-op in production for debug/info levels"
    - "Logger errors and warnings are visible in development builds"
  artifacts:
    - path: "utils/logger.ts"
      provides: "ErrorCategory enum and structured logger with severity levels"
      exports: ["ErrorCategory", "logger"]
      min_lines: 40
    - path: "__tests__/utils/logger.test.ts"
      provides: "Unit tests for logger behavior (severity filtering, category tagging)"
      contains: "describe"
  key_links:
    - from: "utils/logger.ts"
      to: "providers/*.tsx"
      via: "import { logger, ErrorCategory } from '@/utils/logger'"
      pattern: "from '@/utils/logger'"
    - from: "utils/logger.ts"
      to: "services/scryfall.ts"
      via: "import { logger } from '@/utils/logger'"
      pattern: "logger\\.(debug|info|warn|error)"
---

<objective>
Create a structured logger with typed error categories and replace all empty catch blocks and console.log calls across the codebase with classified logger calls.

Purpose: Eliminate silent error swallowing (STAB-04) by establishing a single, typed logging utility that follows the PrinterAdapterError pattern already proven in the printer subsystem. Every catch block must either handle the error or log it with a classified category — no silent swallowing.

Output: `utils/logger.ts` with ErrorCategory enum and severity-gated logger; all 8 empty catch blocks replaced with classified logger calls; all 29 console.log calls replaced with logger calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-security/01-CONTEXT.md
@.planning/phases/01-stability-security/01-RESEARCH.md

<interfaces>
<!-- Existing pattern to replicate — PrinterAdapterError + PrinterErrorCode -->

From services/printer/adapters/port.ts:
```typescript
export enum PrinterErrorCode {
  CONNECTION_FAILED = 'CONNECTION_FAILED',
  DISCOVERY_FAILED = 'DISCOVERY_FAILED',
  SEND_FAILED = 'SEND_FAILED',
  TRANSPORT_MISMATCH = 'TRANSPORT_MISMATCH',
}

export class PrinterAdapterError extends Error {
  code: PrinterErrorCode;
  cause?: Error;
}
```

From providers/SettingsProvider.tsx:
```typescript
export const [SettingsProvider, useSettings] = createContextHook(() => { ... });
export async function getPrinterPreferencesFromSettings(): Promise<PrinterPreferences>;
export async function savePrinterPreferencesToSettings(prefs: Partial<PrinterPreferences>): Promise<void>;
```

From providers/HistoryProvider.tsx:
```typescript
export const [HistoryProvider, useHistory] = createContextHook(() => { ... });
export function useFilteredHistory(search: string, typeFilter?: string, cmcFilter?: number);
```

From types/index.ts:
```typescript
export interface AppSettings {
  excludeDigitalOnly: boolean;
  excludeFunnySets: boolean;
  uniqueCardsOnly: boolean;
  devMode: boolean;
  printer: PrinterPreferences;
}
export const DEFAULT_PRINTER_PREFERENCES: PrinterPreferences;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structured logger with ErrorCategory enum</name>
  <files>utils/logger.ts, __tests__/utils/logger.test.ts</files>
  <behavior>
    - Test 1: logger.error logs with [STORAGE] prefix when ErrorCategory.Storage is used
    - Test 2: logger.warn logs with [NAVIGATION] prefix when ErrorCategory.Navigation is used
    - Test 3: logger.debug is a no-op when __DEV__ is false
    - Test 4: logger.info is a no-op when __DEV__ is false
    - Test 5: logger.error and logger.warn always produce output regardless of __DEV__
    - Test 6: Logger never throws even if console.error itself throws
  </behavior>
  <action>
Per D-01: Create `utils/logger.ts` — a lightweight structured logger (~50 lines) that wraps `console.error`/`console.warn` in development and gates `debug`/`info` levels by `__DEV__`. In production, `error` and `warn` still log; `debug` and `info` are no-ops.

Per D-02: Define `ErrorCategory` enum following the PrinterErrorCode pattern from `services/printer/adapters/port.ts`. Categories: `Storage`, `Network`, `Navigation`, `Printer`, `Render`.

Implementation:
1. Define `ErrorCategory` enum with values: `storage`, `network`, `navigation`, `printer`, `render`.
2. Define `LogLevel` type: `'error' | 'warn' | 'info' | 'debug'`.
3. Create internal `log()` function that:
   - Returns early for `debug`/`info` when `__DEV__` is false
   - Formats prefix as `[CATEGORY]` (uppercase)
   - Wraps each console call in try-catch to prevent logger crashes (per RESEARCH.md Pitfall 2)
   - Uses `console.error` for error level, `console.warn` for warn, `console.log` for info/debug
4. Export `logger` object with `error()`, `warn()`, `info()`, `debug()` methods, each accepting `(category: ErrorCategory, message: string, error?: unknown)`.
5. Write unit tests in `__tests__/utils/logger.test.ts` covering the 6 behaviors above. Mock `__DEV__` using `jest.mock` or `jest.replaceProperty` to test both environments.

DO NOT add a network transport, file logging, or any external dependency. This is a thin wrapper around console methods.
  </action>
  <verify>
    <automated>bun run test -- --testPathPattern="utils/logger" -u</automated>
  </verify>
  <done>utils/logger.ts exports ErrorCategory enum (5 values) and logger object (4 methods). All 6 test behaviors pass. Logger is defensive (never throws) and respects __DEV__ gating.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all empty catch blocks and console.log calls with classified logger calls</name>
  <files>services/scryfall.ts, providers/NetworkProvider.tsx, providers/SettingsProvider.tsx, providers/HistoryProvider.tsx, i18n/index.ts, app/(tabs)/settings/index.tsx, app/(tabs)/settings/printer.tsx, app/(tabs)/(home)/index.tsx, app/card.tsx, app/print-preview.tsx, services/printer/adapters/native.ts, services/printer/adapters/fake.ts, jest.setup.js</files>
  <action>
Per D-03: Replace the 8 identified empty catch blocks with classified logger calls. Classification by intent:

1. `app/(tabs)/settings/index.tsx` — `Linking.openURL` catch: `logger.warn(ErrorCategory.Navigation, 'Failed to open external URL', error)` (per D-03: navigation = warn)
2. `app/(tabs)/settings/printer.tsx` — `mergeDiscoveredWithRegistry` catch: `logger.warn(ErrorCategory.Printer, 'Background registry merge failed', error)` (per D-03: printer = warn)
3. `app/(tabs)/(home)/index.tsx` — empty catch (line ~270): `logger.warn(ErrorCategory.Network, 'Hero art prefetch failed', error)` (prefetch failure is expected, non-blocking)
4. `i18n/index.ts` — `AsyncStorage.setItem` catch (line ~126): `logger.error(ErrorCategory.Storage, 'Failed to persist locale', error)` (per D-03: storage = error)
5-8. `app/(tabs)/settings/printer.tsx` — printer catches (lines ~204, 240, 275, 280): `logger.warn(ErrorCategory.Printer, '<specific message>', error)` (per D-03: printer = warn)

Per D-01: Replace all 29 console.log calls:
- `services/scryfall.ts` — 20 calls: Replace each `console.log('[Scryfall]', ...)` with `logger.debug(ErrorCategory.Network, '<message>', ...)` (request/response traces are debug-level, off in production per RESEARCH.md Open Question 3)
- `providers/NetworkProvider.tsx` — line 73: `logger.info(ErrorCategory.Network, 'Online status changed', { isOnline })`
- `services/printer/adapters/native.ts` — line 94: `logger.debug(ErrorCategory.Printer, 'Discovered devices count', { count })`
- `services/printer/adapters/fake.ts` — line 88: `logger.debug(ErrorCategory.Printer, 'Fake adapter activity', { message })`
- `app/(tabs)/settings/printer.tsx` — lines 343, 347: `logger.debug(ErrorCategory.Printer, '<message>')`
- `app/card.tsx` — any remaining console.log: `logger.debug(ErrorCategory.Render, '<message>')`
- `app/print-preview.tsx` — any remaining console.log: `logger.debug(ErrorCategory.Printer, '<message>')`

Also update `jest.setup.js`: Remove blanket `console.warn` and `console.error` suppression (lines 7-10). Replace with targeted suppression for specific known noisy warnings only. This ensures logger output is visible during test runs (per RESEARCH.md Pitfall 3). If specific tests produce noisy output, add per-test `jest.spyOn(console, 'error').mockImplementation()` instead.

DO NOT double-wrap route param JSON.parse calls in card.tsx/print-preview.tsx — they already have try-catch and are not persisted data (per RESEARCH.md Pitfall 6). Only add logger calls inside existing catch blocks there if they are currently empty.

DO NOT touch `__tests__/` test files — test-only console.log calls are acceptable.
  </action>
  <verify>
    <automated>grep -r "console\\.log\\|console\\.warn\\|console\\.error" --include="*.ts" --include="*.tsx" app/ services/ providers/ i18n/ utils/ | grep -v "node_modules" | grep -v "__tests__" | grep -v "logger.ts" | grep -v ".d.ts" | wc -l</automated>
    Returns 0 or near-zero (only remaining should be in logger.ts itself).
  </verify>
  <done>All 8 empty catch blocks replaced with classified logger calls. All 29 console.log calls in production code replaced with logger calls. jest.setup.js no longer globally suppresses console.warn/console.error. Running `grep -r "console.log" app/ services/ providers/ i18n/ utils/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "logger.ts"` returns zero matches outside logger.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logger output -> console | Console output may leak sensitive data in development builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | utils/logger.ts | accept | Debug/info logs only appear in __DEV__ builds; production builds only emit error/warn. No PII is logged. |
| T-01-02 | Denial of Service | utils/logger.ts | mitigate | Logger wraps console calls in try-catch — never throws even if console is broken (per Pitfall 2) |
</threat_model>

<verification>
1. `bun run test -- --testPathPattern="utils/logger" -u` — Logger unit tests pass
2. `bun run test` — Full test suite passes (no regressions from catch block changes)
3. `grep -r "console.log" app/ services/ providers/ i18n/ utils/ --include="*.ts" --include="*.tsx" | grep -v "__tests__" | grep -v "logger.ts"` — Zero production console.log calls outside logger
4. `grep -r "\.catch(() => {})" app/ services/ providers/ i18n/ --include="*.ts" --include="*.tsx"` — Zero empty arrow-function catches
5. `grep -r "catch {}" app/ services/ providers/ i18n/ --include="*.ts" --include="*.tsx"` — Zero empty catch blocks
</verification>

<success_criteria>
- ErrorCategory enum exists with 5 values: storage, network, navigation, printer, render
- Logger exports error/warn/info/debug methods, all gated by __DEV__ where appropriate
- Zero empty catch blocks remain in production code
- Zero console.log calls remain in production code (outside logger.ts and __tests__)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-security/01-01-SUMMARY.md`
</output>